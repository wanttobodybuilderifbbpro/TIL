## TIL 2023. 06 04 dimanche(sun)


> 다음 코드의 실행 결과는?

```
let myNumber = 2.37e-2

console.log(myNumber.toFixed(2));
```


```
알파벳 e를 활용해서 표기하는 방식을 지수표기법이라고 부르고,

알파벳 'e'의 왼쪽에 있는 숫자에 오른쪽에 있는 수만큼 10의 거듭제곱을 곱하는 의미가 담겨 있습니다.

만약 알파벳 e의 오른쪽 값이 음수일 경우에는 이 숫자만큼 10의 거듭제곱으로 나누는 의미가 되는데요.

그래서 지수표기법으로 표기된 2.37e-2를 풀어서 작성해보면, 0.0237 입니다.

그리고 toFixed 메소드는 파라미터에 전달된 값만큼의 소수점 자릿수를 고정적으로 표기하는 메소드인데요. 파라미터에 전달된 값보다 소수점 자릿수가 더 많은 경우에는 바로 다음 자릿수에서 반올림됩니다.

따라서 0.0237을 소수점 아래 두 번째 자리까지 표기해야하니 세 번째 자리에서 반올림하면,

정답은 (4) 0.02가 됩니다.
```



> 다음 코드 중 숫자 123을 표현하는 코드가 아닌 것은?

```

1 - Number((4 * 30 + 3).toString(10));

2 (123.456).toFixed(0);

3 1.23e2;

4 0x7b.toString(10) * 1;

```

```


1 - 일단 시작부터Number함수를 통해 숫자로 형 변환을 하고 있는데요 내부를 좀 더 살펴보면, 숫자연산을 통해 123을 만든 다음 toString 메소드를 통해 10진수로 변환을 하고 있습니다. 
만약 이 내부만 작성했다면 결과는 문자열 '123'이였겠지만, 결과적으로는 Number를 통해 숫자로 다시 형 변환이 되죠? 
그래서 이 코드는 숫자 123을 표현하는 코드 입니다.

2 - 숫자 123.456을 toFixed 메소드를 활용해서 소수점 아래 값들을 모두 지워주었습니다. 
하지만, toFixed 메소드도 toString과 마찬가지로 결괏값이 문자열로 리턴되는데요. 문자 자체는 123이지만, 이 값은 숫자 123이 아닌 문자열 '123'이 됩니다.
따라서 정답은 2번입니다.

3 - 1.23e2이라는 지수표기법을 해석해보면 숫자 1.23에 10을 두 번 곱하는 것과 같기 때문에 이 코드는 숫자 123을 표현하는 코드가 됩니다.

4 - 0x7b는 숫자 123을 16진수로 표현한 코드입니다. toString 메소드로 10진법으로 표기를 바꿨는데, 그 뒤에 1을 곱해주고 있죠? toString 메소드의 결과로 문자열 '123' 되었지만 바로 뒤에 1을 곱해주면서, 문자열 '123'이 숫자 123으로 형 변환이 되어 계산됩니다. 
결과적으로 이 코드는 숫자 123을 표현하는 코드가 됩니다.



```







```

절댓값(Absolute Number)

Math.abs(x)


최댓값(Maximum)

Math.max(x)

최솟값(Minimum)

Math.min(x)

거듭제곱(Expectation)

Math.pow(x)

제곱근(Square Root)

Math.sqrt(x);

반올림(Round)

Math.round(x)


```
